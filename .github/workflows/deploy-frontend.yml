name: Deploy Pequefest.com

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    - name: Prepare deployment files
      run: |
        mkdir -p deploy
        cp party-rentals-frontend/index.html deploy/
        cp party-rentals-frontend/reservas.html deploy/
        cp party-rentals-frontend/admin.html deploy/
        cp party-rentals-frontend/favicon.svg deploy/
        cp -r party-rentals-frontend/components deploy/
        cp -r party-rentals-frontend/src deploy/
        
    - name: Upload to GitHub Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: deploy
        
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
concurrency:
  group: "pages"
  cancel-in-progress: false
